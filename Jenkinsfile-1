pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        sh 'docker immge build -t java-web-app'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy java-web-app'
      }
    }
  }
}
