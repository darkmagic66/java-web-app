pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t java-web-app:latest .'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy image java-web-app:latest'
      }
    }
  }
}
